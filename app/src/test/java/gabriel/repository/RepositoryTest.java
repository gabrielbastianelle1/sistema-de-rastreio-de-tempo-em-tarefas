package gabriel.repository;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;

import gabriel.core.user.domain.User;
import gabriel.core.user.repository.UserRepository;

public class RepositoryTest {

    // @ParameterizedTest
    // @ValueSource(classes = { Database.class })
    // public void testSave(Class<? extends UserRepository> clazz) throws Exception
    // {
    // UserRepository userRepository = clazz.getDeclaredConstructor().newInstance();
    // userRepository.save(new User("gabriel", "123"));

    // }

}
